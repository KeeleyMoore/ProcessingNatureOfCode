/* autogenerated by Processing revision 1276 on 2021-10-09 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class CustomDistributionValueSquared extends PApplet {

// Use a custom probability distribution to vary the size of a step taken by the random walker. 
// The step size can be determined by influencing the range of values picked. 
// Can you map the probability exponentially—i.e. making the likelihood that a value is picked equal to the value squared?
Random generator;
Walker cd;
 public void setup() {
  /* size commented out by preprocessor */;
  frameRate(30);

  // Create a walker object
  cd = new Walker();

  background(255);
}

 public void draw() {
  // background(255);
  // Run the walker object
  cd.step();
  cd.render();
}

class Walker {
  int x;
  int y;

  Walker() {
    x = width / 2;
    y = height / 2;
    
    generator = new Random();
  }
  
  
   public void render() {
    stroke(130, 70, 90);
    point(x, y);
  }
  
   public void step() {
    float stepSize = stepSize(0,10);
    
    float stepx = random( -stepSize,stepSize);
    float stepy = random( -stepSize,stepSize);
    
    x += stepx;
    y += stepy;
  }
  
   public float stepSize(int start, int end) {
    while(true) {
      float stepSize = random(start,end);
      float stepSquared = sqrt(stepSize);
      float probability = random(0, stepSquared);
      
      if (stepSize > probability) {
        return stepSize;
      }
    }
  }
}


  public void settings() { size(400, 400); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "CustomDistributionValueSquared" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
