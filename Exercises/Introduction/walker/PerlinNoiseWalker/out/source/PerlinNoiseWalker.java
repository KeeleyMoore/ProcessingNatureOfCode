/* autogenerated by Processing revision 1276 on 2021-10-09 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class PerlinNoiseWalker extends PApplet {

Walker w;

 public void setup() {
  /* size commented out by preprocessor */;
  background(255);

  w = new Walker();
}

 public void draw() {
  w.render();
  w.step();
}
class Walker {
  float x,y;
  float tx,ty;
  Walker() {
    tx = 0;
    ty = 10000;
  }
  
   public void render() {
    stroke(0);
    point(x, y);
  }
  
   public void step() {
    // x - and y - location mapped from noise;
    x = map(noise(tx), 0, 1, 0, width);
    y = map(noise(ty), 0, 1, 0, height);
    
    // Moveforward through “time.”
    tx += 0.01f;
    ty += 0.01f;
  }
}


  public void settings() { size(800, 400); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "PerlinNoiseWalker" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
